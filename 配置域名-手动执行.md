# 配置域名 hello1984.net - 手动执行指南

## 方法一：使用提供的脚本（推荐）

### 步骤1：上传脚本到服务器

在PowerShell中执行（会提示输入密码，请输入: `0iHSn3CpCpDmlkub`）：

```powershell
scp -o StrictHostKeyChecking=no domain-setup.sh root@38.175.195.104:/tmp/
```

### 步骤2：SSH连接服务器并执行脚本

```powershell
ssh -o StrictHostKeyChecking=no root@38.175.195.104
```

连接后执行：

```bash
chmod +x /tmp/domain-setup.sh
bash /tmp/domain-setup.sh
```

## 方法二：直接SSH执行命令

如果您已经SSH连接到服务器，可以直接执行以下命令：

```bash
# 备份现有配置（如果存在）
if [ -f /etc/nginx/sites-available/maclock ]; then
    cp /etc/nginx/sites-available/maclock /etc/nginx/sites-available/maclock.backup.$(date +%Y%m%d_%H%M%S)
fi

# 创建Nginx配置文件
cat > /etc/nginx/sites-available/maclock <<'EOF'
server {
    listen 80;
    listen [::]:80;
    server_name hello1984.net www.hello1984.net;

    access_log /var/log/nginx/maclock-access.log;
    error_log /var/log/nginx/maclock-error.log;

    client_max_body_size 10M;

    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript application/x-javascript application/xml+rss application/json application/javascript;

    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection 'upgrade';
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_cache_bypass $http_upgrade;

    proxy_connect_timeout 60s;
    proxy_send_timeout 60s;
    proxy_read_timeout 60s;

    location / {
        proxy_pass http://localhost:3000;
        proxy_redirect off;
    }

    location ~* \.(jpg|jpeg|png|gif|ico|css|js|svg|webp|avif|woff|woff2|ttf|eot)$ {
        proxy_pass http://localhost:3000;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
EOF

# 创建符号链接
ln -sf /etc/nginx/sites-available/maclock /etc/nginx/sites-enabled/maclock

# 测试配置
nginx -t

# 重启Nginx
systemctl restart nginx

# 检查状态
systemctl status nginx
```

## 验证配置

配置完成后，执行以下命令验证：

```bash
# 检查Nginx配置
nginx -t

# 检查Nginx状态
systemctl status nginx

# 测试域名访问
curl -I http://hello1984.net
```

## 重要提示

1. ✅ **DNS解析**：确保域名 `hello1984.net` 的DNS A记录已指向服务器IP `38.175.195.104`
2. ✅ **防火墙**：确保防火墙已开放80端口
3. ✅ **云服务器安全组**：如果使用云服务器，请在控制台配置安全组规则，允许80端口入站流量
4. ✅ **Next.js应用**：确保Next.js应用正在3000端口运行

## 配置完成后

访问 `http://hello1984.net` 应该能看到您的网站。

如果需要配置HTTPS（443端口），可以使用Let's Encrypt免费SSL证书。



